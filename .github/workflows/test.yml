name: Shell Commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo name
        run: echo "Hello Merine"
      - name: CHeck version
        run: npm -v



